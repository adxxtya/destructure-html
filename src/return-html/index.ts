import { findTagByClass, findTagByText } from "./Traverse";
import { getContentBetweenTags } from "./Linear";

export const Traverse = {
  findTagByClass,
  findTagByText,
};

export const Linear = {
  getContentBetweenTags,
};
